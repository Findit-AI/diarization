[project]
name = "dia-parity-reference"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
  "pyannote.audio == 4.0.4",
  "pyannote.metrics >= 3.2",
  "numpy >= 1.26",
]

[tool.setuptools]
packages = []
