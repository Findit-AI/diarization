//! Filled in by Tasks 10-12.
#![allow(dead_code)]
// TODO(task-11): replace stub with real Clusterer
use crate::cluster::ClusterOptions;
pub struct Clusterer {
  _opts: ClusterOptions,
}
impl Clusterer {
  pub fn new(opts: ClusterOptions) -> Self {
    Self { _opts: opts }
  }
}
